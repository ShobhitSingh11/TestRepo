---
kind: pipeline
type: docker
name: default

platform:
  os: linux
  arch: arm64
  
steps:
- name: another
  image: alpine
  commands:
  - echo "Hello how r u. Womp womp"
  - sleep 2
  - echo "Done"
