---
kind: pipeline
type: docker
name: default

platform:
  os: linux
  arch: amd64
  
steps:
- name: another
  image: alpine
  commands:
  - echo "hello"

trigger:
  ref:
    - refs/heads/main
    - refs/tags/**
    - refs/pull/**
