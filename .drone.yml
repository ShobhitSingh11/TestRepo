---
kind: pipeline
type: docker
name: default

platform:
  os: linux
  arch: arm64
  
steps:
- name: another
  image: alpine
  commands:
  - yes "This is a test log line" | head -n 12001


trigger:
  ref:
    - refs/heads/main
    - refs/tags/**
    - refs/pull/**   

---
kind: signature
hmac: F10E2821BBBEA527EA02200352313BC059445190
